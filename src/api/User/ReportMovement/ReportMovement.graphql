type ReportMovementReponse {
    ok: Boolean!
    error: String
}

type Mutation {
    ReportMovement(
        lastOrientation: Float
        lastLat: Float
        lastLng: Float
    ): ReportMovementReponse!
}
